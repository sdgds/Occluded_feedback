# 脉冲神经网络（SNN）的训练方法综述

**覆盖：E‑prop、RTRL、ES‑D‑RTRL 和 树突电压代理方法的原理、数学推导与对比**

---

## 1. 问题背景：SNN 训练的挑战

脉冲神经网络（SNN）不同于传统的连续激活神经网络，其输出是**离散的脉冲**（spike）。由于离散性质及时间依赖性，直接应用传统的反向传播（如 BPTT）会遇到难题：

* 脉冲输出不可导；
* 历史响应影响未来输出；
* 网络是动态系统：当前状态依赖于整个过去的输入序列。

因此出现了三种主要训练范式：
📌 **E‑prop**：基于事件的近似梯度方法
📌 **RTRL**：实时递归梯度方法
📌 **ES‑D‑RTRL**：对 RTRL 的可扩展近似优化
📌 **树突电压代理**：基于树突模型的精确梯度计算方法

---

## 2. E‑prop：事件驱动的在线学习

### 2.1 基本思想

**E‑prop**（Event‑driven Propagation）是一种在线、事件驱动的学习方法，主要用于 **SNN**。
核心在于：

1. **仅在脉冲（spike）发生时计算更新**；
2. 利用膜电位（membrane potential）导数作为伪梯度近似；
3. 引入 **资格迹（Eligibility Trace）** 来处理时间依赖性。

---

### 2.2 神经元动态模型（LIF 为例）

膜电位 $V_t$ 在时间上递归更新，例如（简化表达）：

$$
V_t = \alpha V_{t-1} + W x_t - \theta y_{t-1}
$$

输出脉冲：

$$
y_t = H(V_t - \theta)
$$

其中 $H(\cdot)$ 是阶跃函数。

由于脉冲不可导，采用 **伪梯度**（surrogate gradient）：

$$
\frac{\partial y_t}{\partial V_t} \approx \sigma'(V_t)
$$

一个常见伪梯度形式：

$$
\sigma'(V_t) = \frac{1}{\beta} \max(0, 1 - |V_t - \theta|)
$$

---

### 2.3 资格迹（Eligibility Trace）

为了补偿过去状态对当前梯度的影响，引入 **资格迹 $e_t$**：

$$
e_t = \tau e_{t-1} + \frac{\partial y_t}{\partial V_t} \cdot \frac{\partial V_t}{\partial W}
$$

这里：

* $e_t$ 记录历史活动对权重影响的“资格”；
* $\tau$ 是衰减因子；
* $\frac{\partial V_t}{\partial W}$ 用来衡量参数对当前膜电位的影响。

**物理意义**

* 膜电位越频繁或越接近阈值，资格迹越大；
* 大资格迹表示该神经元该连接对当前输出更具有“责任”。

---

### 2.4 梯度计算与权重更新

损失函数 $L$ 对权重的梯度：

$$
\frac{\partial L}{\partial W}
= \sum_{t} \frac{\partial L}{\partial y_t}\cdot \frac{\partial y_t}{\partial V_t}\cdot e_t
$$

权重更新：

$$
\Delta W = -\eta \sum_t \delta_t \cdot e_t
$$

其中：

$$
\delta_t = \frac{\partial L}{\partial y_t}
$$

**要点总结**
✔ 事件驱动：仅在脉冲或有意义事件时更新
✔ 不需要时间展开整个序列
✔ 利用膜电位伪梯度和资格迹处理时间依赖性

---

## 3. RTRL：实时递归学习

### 3.1 基本思想

**RTRL**（Real‑Time Recurrent Learning）是**实时更新**的递归梯度算法，专门设计来处理递归神经网络和 SNN。
**RTRL** 不依赖事件触发，而是 **在每个时间步** 维护完整梯度结构。

---

### 3.2 全量梯度（Jacobian）

网络状态向量 $h_t$（例如膜电位）由递归关系定义：

$$
h_t = f(h_{t-1}, x_t, W)
$$

**RTRL** 需要计算：

$$
\frac{\partial h_t}{\partial W}
$$

这是一个 **雅可比矩阵**（Jacobian）:

$$
J_t = \frac{\partial h_t}{\partial W} \in \mathbb{R}^{N \times |\!W\!|}
$$

表示每个神经元状态对每个权重的导数（全量）。

---

### 3.3 递归公式

通过递归展开（chain rule）：

$$
J_t = \frac{\partial f}{\partial h_{t-1}} J_{t-1} + \frac{\partial f}{\partial W}
$$

这意味着每步更新都需要：

1. 保留前一时刻的全量雅可比矩阵 $J_{t-1}$
2. 计算当前状态对权重和状态的偏导数

损失对权重的梯度：

$$
\frac{\partial L}{\partial W}
= \sum_{t} \frac{\partial L}{\partial h_t} J_t
$$

---

### 3.4 全量梯度存储的解释

**RTRL** 中的全量梯度指的就是 **每个神经元状态对每个权重的导数**：

$$
 J_t[i,j] = \frac{\partial h_t[i]}{\partial W[j]}
$$

因此 **RTRL** 需要存储一个维度为 $N \times N$ 的矩阵，并递归更新。若权重维度与神经元数目同阶，则内存量为：

$$
O(N^2)
$$

这对于大规模网络是不可接受的。

---

## 4. ES‑D‑RTRL：可扩展的 RTRL

### 4.1 发展动机

**RTRL** 是精确的，但：

* 内存 $O(N^2)$
* 计算量也很大

**ES‑D‑RTRL**（Exponential Smoothing Diagonal RTRL）提出以下两个关键近似：

1. **对角矩阵近似**
2. **指数平滑**

目标：在保留 **RTRL** 实时性的基础上显著降低内存和计算开销。

---

### 4.2 对角雅可比近似

**RTRL** 中的 **雅可比矩阵**：

$$
J_t = \frac{\partial h_t}{\partial W}
$$

是全量矩阵，包含神经元之间的交互。

**对角近似** 的想法是：

$$
J_t \approx \text{diag}(J_t)
$$

即保留主对角线元素：

$$
J_t[i,j] \approx
\begin{cases}
\frac{\partial h_t[i]}{\partial W[j]}, & \text{if } i=j\\
0, & \text{otherwise}
\end{cases}
$$

**意义**

* 仅保留每个神经元状态对 **自身权重的影响**；
* 忽略神经元之间的相互影响项。

**注意**
这不是说“神经元之间不交互”，而是做了**近似**以便在计算和存储上可行。实践中对大规模系统仍能有效学习。

---

### 4.3 指数平滑

为了进一步减少内存开销，**ES‑D‑RTRL** 引入 **指数平滑** 来递推梯度，而不是存储每个时间步完整的数据。

定义平滑梯度：

$$
G_t = \alpha G_{t-1} + (1-\alpha) J_t
$$

其中 $0 < \alpha < 1$ 控制平滑的强度。

因此不再需要存保留完整历史梯度，仅需缓存当前 smoothed gradient $G_t$。

---

### 4.4 ES‑D‑RTRL 梯度计算

结合上述近似与平滑，损失对权重的梯度近似为：

$$
\frac{\partial L}{\partial W}
\approx \sum_t \frac{\partial L}{\partial h_t} \cdot \big(\text{diag}(G_t)\big)
$$

其中：

* $\text{diag}(G_t)$ 是经过平滑的对角雅可比近似
* 不再需要大规模全权重梯度矩阵

---

## 5. 核心对比

| 方法            | 梯度精度     | 是否基于事件 | 内存复杂度       | 计算量 | 优势             | 劣势          |
| ------------- | -------- | ------ | ----------- | --- | -------------- | ----------- |
| **BPTT**      | 精确（展开时间） | 否      | $O(TN)$（时间） | 高   | 全序列全梯度         | 非实时         |
| **E‑prop**    | 伪梯度 + 局部 | 是      | $O(N)$      | 低   | 生物启发、事件驱动      | 需伪梯度近似      |
| **RTRL**      | 精确递归     | 否      | $O(N^2)$    | 很高  | 实时精确梯度         | 内存/计算爆炸     |
| **ES‑D‑RTRL** | 近似递归     | 否      | $O(N)$      | 中等  | 更大规模可行         | 精度较 RTRL 降低 |
| **树突电压代理**    | 精确（无伪梯度） | 否      | $O(N)$      | 中等  | 树突计算能力可解释、精确梯度 | 更复杂的数学推导    |

---

## 6. 核心公式总结

### E‑prop

状态更新：

$$
h_t = f(h_{t-1}, x_t)
$$

资格迹：

$$
e_t = \tau e_{t-1} + \frac{\partial y_t}{\partial V_t} \cdot \frac{\partial V_t}{\partial W}
$$

梯度：

$$
\frac{\partial L}{\partial W} = \sum_t \frac{\partial L}{\partial y_t} \cdot \frac{\partial y_t}{\partial V_t} \cdot e_t
$$

### RTRL

递推雅可比：

$$
J_t = \frac{\partial f}{\partial h_{t-1}} J_{t-1} + \frac{\partial f}{\partial W}
$$

梯度：

$$
\frac{\partial L}{\partial W} = \sum_{t} \frac{\partial L}{\partial h_t} \cdot J_t
$$

### ES‑D‑RTRL

对角近似：

$$
J_t \approx \text{diag}(J_t)
$$

指数平滑：

$$
G_t = \alpha G_{t-1} + (1-\alpha) J_t
$$

近似梯度：

$$
\frac{\partial L}{\partial W}
\approx \sum_{t} \frac{\partial L}{\partial h_t} \cdot \big(\text{diag}(G_t)\big)
$$

### 树突电压代理

树突电压代理：

$$
V^*(t) = \sum_{k=1}^N K_{jk}(t) * (I_k(t) + E_{\text{rest}})
$$

梯度计算：

$$
\frac{\partial L}{\partial W} = \sum_{t=1}^T \frac{\partial L}{\partial V^*(t)} \cdot \frac{\partial V^*(t)}{\partial K_{jk}(t)}
$$

---

## 7. 物理与生物学意义

| 概念              | 生物意义                     |
| --------------- | ------------------------ |
| 资格迹（E‑prop）     | 类似突触的“记忆痕迹”：过去活动对当前学习有影响 |
| 雅可比矩阵（RTRL）     | 精确追踪神经元间所有影响             |
| 对角近似（ES‑D‑RTRL） | 假设局部贡献为主，其它交互可以容忍近似      |
| 指数平滑            | 类似神经适应或衰减记忆              |
| 树突电压代理          | 结合被动树突与主动电流：简化神经元电压的计算   |

---

## 8. 什么时候用哪个方法？

* **E‑prop**：适用于脉冲神经网络、事件驱动情境、需要高效在线学习。
* **RTRL**：需要精确递归梯度、规模较小的网络，可用于分析性实验。
* **ES‑D‑RTRL**：大规模实时系统；在性能与效率之间取一个均衡。
* **树突电压代理**：适用于生物物理模型、树突计算能力的建模，结合主动电流的梯度计算。

---

如有需要，我可以进一步补充示例推导、伪代码以及实际应用场景的具体实现解释。欢迎继续提问！
